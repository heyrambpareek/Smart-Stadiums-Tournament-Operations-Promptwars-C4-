export default {
  plugins: {
    tailwindcss: {},
    /* autoprefixer temporarily disabled to avoid caniuse-lite runtime errors
       during local dev. Re-enable after running `npm install` to update
       PostCSS/browser data packages. */
    // autoprefixer: {},
  },
}
